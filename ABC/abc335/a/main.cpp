#include <bits/stdc++.h>
#include <atcoder/all>
using namespace std;
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define ll long long

int main() {
    string s;
    cin >> s;
    rep(i, s.length()-1) {
        cout << s[i];
    }
    cout << 4 << endl;
    return 0;
}