#include <bits/stdc++.h>
using namespace std;


const int N = 200010;
const int M = 200010;
vector<vector<int>> box(N, vector<int>());
vector<vector<int>> card(M, vector<int>());

int main() {
    int n, q;
    cin >> n >> q;

    int t, i, j;

    while (q--){
        cin >> t;
        switch (t)
        {
        case 1:
            box[j].push_back(i)
            break;
        
        default:
            break;
        }
    }
    
}